{
  "name": "Assessment Platform Codespace",
  "build": {
    "dockerfile": "devcontainer.Dockerfile",
    "context": ".."
  },
  "postCreateCommand": "bash .devcontainer/setup.sh",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
      },
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "ms-azuretools.vscode-docker"
      ]
    }
  },
  "forwardPorts": [3000, 5173, 6379, 27017, 15672],
  "portsAttributes": {
    "3000": { "label": "Backend API" },
    "5173": { "label": "Frontend" },
    "6379": { "label": "Redis" },
    "27017": { "label": "MongoDB" },
    "5672": { "label": "RabbitMQ" }
  },

  // ✅ Fix: run as root (since vscode user doesn’t exist)
  "remoteUser": "root",
  "updateRemoteUserUID": false,

  // Start Docker Compose automatically after attaching
  "postAttachCommand": "docker-compose up --build"
}
